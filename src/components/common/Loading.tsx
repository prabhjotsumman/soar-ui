const Loading = () => (
  <div className="flex items-center justify-center h-full font-medium text-xl bg-gray-50 text-gray-600 animate-pulse rounded-lg shadow-sm">
    Loading...
  </div>
);

export default Loading;
